services:
  - type: web
    name: mongodb-bot
    runtime:
      version: '3.9.5'
    env:
      API_ID: ${API_ID}
      API_HASH: ${API_HASH}
      BOT_TOKEN: ${BOT_TOKEN}
    ports:
      - port: 80
        targetPort: 8080
    healthcheck:
      http:
        path: /
